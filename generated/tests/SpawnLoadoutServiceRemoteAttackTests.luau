--!strict
-- <auto-generated by luau-contract. Do not edit by hand.>
-- luau-contract:remote-attack-tests:v1:5dc04725d5d6be9bb4eb43b2e7a9a160d848dd401f684e3192916fd84c3e1857

local Contracts = require("../../src/Contracts")
local ContractModule = require("../../examples/spawn_loadout.contract")
local Contract = ContractModule.Contract or ContractModule

local function largeString(length: number): string
	return string.rep("A", length)
end

return function(check)
	do -- SpawnLoadoutService.SpawnRequest
		local validActor = {
			Name = "ValidPlayer",
			UserId = 1,
		}
		local defaultResponses = {
			SpawnPlayer = {
				spawned = true,
			},
		}
		local function newHarness(defaultResponsesOverride)
			local harness = Contracts.Test.remoteHarness(Contract, {
				defaultResponses = defaultResponsesOverride or defaultResponses,
			})
			harness:implement("SpawnPlayer")
			harness:bind("SpawnRequest", {
		context = {},
		states = {
			Spawn = "Menu",
		},
	})
			return harness
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, nil)
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects missing payload", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for missing payload", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, "not_object")
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects wrong payload type", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for wrong payload type", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {
				Mode = "__invalid_choice__",
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects invalid Mode", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for invalid Mode", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {
				NewSession = "not_boolean",
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects invalid NewSession", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for invalid NewSession", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {
				SpawnPointId = 12345,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects invalid SpawnPointId", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for invalid SpawnPointId", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {
				SpawnPointId = largeString(81),
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects pathological SpawnPointId", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for pathological SpawnPointId", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {
				__unexpected = true,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects extra field", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records invalid payload for extra field", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", nil, {})
			local actorDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rejects missing actor", harness:handlerCalls("SpawnPlayer") == beforeCalls)
			check("SpawnLoadoutService.SpawnRequest records actor rejection", actorDiagnostic ~= nil and string.find(actorDiagnostic.name, "Actor", 1, true) ~= nil)
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			for _ = 1, 5 do
				harness:call("SpawnRequest", validActor, {})
			end
			local rateLimitDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest rate limits spam", rateLimitDiagnostic ~= nil and rateLimitDiagnostic.name == "RemoteRateLimited")
		end
	
		do
			local badResponses = {
				SpawnPlayer = "not_object",
			}
			local harness = newHarness(badResponses)
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("SpawnPlayer")
			harness:call("SpawnRequest", validActor, {})
			local responseDiagnostic = harness:lastDiagnostic()
			check("SpawnLoadoutService.SpawnRequest reaches handler for bad response shape", harness:handlerCalls("SpawnPlayer") == beforeCalls + 1)
			check("SpawnLoadoutService.SpawnRequest records bad response shape", responseDiagnostic ~= nil and responseDiagnostic.name == "ActionOutputInvalid")
		end
	end
end
