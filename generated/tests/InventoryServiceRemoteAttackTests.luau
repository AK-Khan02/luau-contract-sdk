--!strict
-- <auto-generated by luau-contract. Do not edit by hand.>
-- luau-contract:remote-attack-tests:v1:8bb8d811f04be968e758a048a6a3184240d71e52ae3e026403ebe4a9031da716

local Contracts = require("../../src/Contracts")
local ContractModule = require("../../examples/inventory.contract")
local Contract = ContractModule.Contract or ContractModule

local function largeString(length: number): string
	return string.rep("A", length)
end

return function(check)
	do -- InventoryService.EquipItem
		local validActor = {
			Name = "ValidPlayer",
			UserId = 1,
		}
		local defaultResponses = {
			EquipItem = {
				equipped = true,
				itemId = "ValidId",
			},
		}
		local function newHarness(defaultResponsesOverride)
			local harness = Contracts.Test.remoteHarness(Contract, {
				defaultResponses = defaultResponsesOverride or defaultResponses,
				scheduler = Contracts.Test.manualScheduler(),
			})
			harness:implement("EquipItem")
			harness:bind("EquipItem", {
		context = {},
		states = {},
	})
			return harness
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, nil)
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects missing payload", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for missing payload", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, "not_object")
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects wrong payload type", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for wrong payload type", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				Slot = 1,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects missing ItemId", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for missing ItemId", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = 12345,
				Slot = 1,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects invalid ItemId", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for invalid ItemId", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = largeString(81),
				Slot = 1,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects pathological ItemId", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for pathological ItemId", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = "ValidId",
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects missing Slot", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for missing Slot", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = "not_number",
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects invalid Slot", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for invalid Slot", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = math.huge,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects pathological Slot", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for pathological Slot", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = 1,
				__unexpected = true,
			})
			local payloadDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects extra field", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records invalid payload for extra field", payloadDiagnostic ~= nil and (payloadDiagnostic.name == "ActionInputInvalid" or payloadDiagnostic.name == "RemotePayloadInvalid"))
		end
	
		do
			local harness = newHarness()
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", nil, {
				ItemId = "ValidId",
				Slot = 1,
			})
			local actorDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem rejects missing actor", harness:handlerCalls("EquipItem") == beforeCalls)
			check("InventoryService.EquipItem records actor rejection", actorDiagnostic ~= nil and string.find(actorDiagnostic.name, "Actor", 1, true) ~= nil)
		end
	
		do
			local scheduler = Contracts.Test.manualScheduler()
			local harness = Contracts.Test.remoteHarness(Contract, {
				defaultResponses = defaultResponses,
				scheduler = scheduler,
			})
			harness:implementYielding("EquipItem")
			harness:bind("EquipItem", {
				context = {},
				states = {},
			})
			harness:clearDiagnostics()
			local first = harness:callAsync("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = 1,
			})
			local second = harness:callAsync("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = 1,
			})
			check("InventoryService.EquipItem never interleaves in-flight duplicates", harness:handlerCalls("EquipItem") == 1)
			local busyDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem records ActionBusy for in-flight duplicate", busyDiagnostic ~= nil and busyDiagnostic.name == "ActionBusy")
			while harness:pendingHandlerCount("EquipItem") > 0 do
				harness:resume("EquipItem")
			end
			check("InventoryService.EquipItem settles every duplicate call", first.settled == true and second.settled == true)
		end
	
		do
			local scheduler = Contracts.Test.manualScheduler()
			local harness = Contracts.Test.remoteHarness(Contract, {
				defaultResponses = defaultResponses,
				scheduler = scheduler,
			})
			harness:implementYielding("EquipItem")
			harness:bind("EquipItem", {
				context = {},
				states = {},
			})
			harness:clearDiagnostics()
			local pending = harness:callAsync("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = 1,
			})
			check("InventoryService.EquipItem holds slow handler before deadline", pending.settled == false)
			scheduler.advance(5)
			check("InventoryService.EquipItem times out stuck handler", pending.settled == true)
			check("InventoryService.EquipItem records ActionTimeout", #harness:diagnostics():findByName("ActionTimeout") >= 1)
			while harness:pendingHandlerCount("EquipItem") > 0 do
				harness:resume("EquipItem")
			end
			check("InventoryService.EquipItem blocks commits after timeout", #harness:diagnostics():findByName("ActionCancelled") >= 1)
		end
	
		do
			local badResponses = {
				EquipItem = "not_object",
			}
			local harness = newHarness(badResponses)
			harness:clearDiagnostics()
			local beforeCalls = harness:handlerCalls("EquipItem")
			harness:call("EquipItem", validActor, {
				ItemId = "ValidId",
				Slot = 1,
			})
			local responseDiagnostic = harness:lastDiagnostic()
			check("InventoryService.EquipItem reaches handler for bad response shape", harness:handlerCalls("EquipItem") == beforeCalls + 1)
			check("InventoryService.EquipItem records bad response shape", responseDiagnostic ~= nil and responseDiagnostic.name == "ActionOutputInvalid")
		end
	end
end
